spring:
  datasource:
    url: jdbc:h2:mem:test_task_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enable: true
      path: /h2-console
      settings:
        trace: true
        web-allow-others: true
  jpa:
    hibernate:
      ddl-auto: update